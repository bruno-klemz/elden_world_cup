# Elden Album — Delivery Report

This file tracks what is implemented vs. what depends on assets/decisions from
the project owner. Updated as tasks complete.

## Placeholders in use (need real assets)
- **Boss art:** all bosses currently render `assets/images/malenia.webp` as a
  placeholder. Replace per boss by dropping the file in `assets/images/` and
  updating `art` in `assets/bosses.json`.
- **Base map image:** `assets/images/map/base_map.webp` is a low-res (600×563)
  placeholder of the world map. It works, but: (1) low resolution → blurry when
  zoomed; (2) it has printed item markers that compete with our own pins;
  (3) labels are in French. Replace later with a clean, high-res (2000px+) map
  without printed markers.
- **Loot icons:** `assets/images/loot/*` are placeholders (emoji 💎 fallback).

## What I need from you
(See the final section of this report — kept current as implementation proceeds.)
